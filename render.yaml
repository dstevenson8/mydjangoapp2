services:
  - type: web
    name: mydjangoapp2
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mysite.wsgi:application
    autoDeploy: true
